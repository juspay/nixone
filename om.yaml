health:
  default:
    nix-version:
      supported: ">=2.16.0"
    direnv:
      required: true
    homebrew:
      enable: true
      required: true
    trusted-users:
      enable: true
    # TODO: Add the attic cache after https://github.com/juspay/omnix/issues/474
    # caches:
    #   required:
    #     - https://euler.cachix.org
