health:
  default:
    nix-version:
      supported: ">=2.16.0"
    direnv:
      required: true
    homebrew:
      enable: true
      required: true
    trusted-users:
      enable: true
    caches:
      required:
        - https://euler.cachix.org
